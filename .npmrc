# Silence the `npm warn config production Use --omit=dev instead` emitted
# by Railway's Nixpacks install step, which defaults to the deprecated
# --production flag. `omit=dev` is the forward-compatible spelling and
# matches what Railway actually wants: ship runtime deps + compiled JS,
# skip devDependencies.
omit=dev
