# Static assets served at /assets/*.

- `lga-logo.png` — LGA brand logo (angel wings + halo + wordmark). Rendered in the header (36px) and on the login card (56px). Referenced by `src/views/layout.ts` and `src/views/auth-login.ts`.
